import math

def adder(odd):
	number = raw_input("type number ")
	if num % 2 == 1
	if number == "":
		print "The sum is {}".format(odd)
	else:
		total += float(number)
		adder(total)

def main():
	total = 0 

	adder(total)

	
main()
